

## Plan: Implement Tier-Based Feature Gates

### Overview
Expand the existing `FeatureGate` component with rich per-feature configs, add search usage tracking for Growth tier's 10/month limit, add watermark logic to exports for Starter users, fix stale plan references in `ApiDocs.tsx` and `Opportunities.tsx`, and add an Enterprise CSM badge.

### Changes

**1. Expand `FeatureGate` configs — `src/components/subscription/FeatureGate.tsx`**

Add rich config entries for all gated features with tier-appropriate messaging:
- `opportunity_search`: icon=Search, "Available on Growth plan", CTA "Upgrade to Growth — $199/month"
- `evaluation`: icon=Wand2, "Available on Business plan", CTA "Upgrade to Business — $499/month"
- `api_access`: icon=Key, "Available on Business plan", CTA "Upgrade to Business"
- `team_collaboration`: keep existing, update ROI copy
- `design_studio`: icon=Palette, "Available on Business plan"
- `data_export`: icon=Download, "Available on Growth plan"

Add a `requiredPlan` field to config so the CTA dynamically says "Upgrade to Growth" vs "Upgrade to Business". Add optional `trialCopy` field defaulting to "14-day free trial · Cancel anytime".

**2. Fix Opportunities page gate — `src/pages/Opportunities.tsx`**

- Replace stale `hasPro` variable and "Pro Feature" text with proper tier-aware messaging
- Use `useSubscriptionFeatures` instead of raw `determineFeatureAccess`
- Update locked state: headline "Opportunity Search", message "Available on Growth, Business & Enterprise plans", CTA "Upgrade to Growth — $199/month", secondary "Or start 14-day free trial"

**3. Fix API Docs gate — `src/pages/ApiDocs.tsx`**

- Replace hardcoded `plan_type === "enterprise" || "pro" || "white_label"` check with `useSubscriptionFeatures().hasFeature('api_access')`
- Update locked message: "API Access is available on Business and Enterprise plans"

**4. Gate Proposal Evaluation — `src/components/project/proposal-evaluation/ProposalEvaluation.tsx`**

- Wrap the Evaluate button with a feature check: if `!hasFeature('evaluation')`, show a `FeatureGate` overlay or replace button with locked badge "Business plan required" + CTA

**5. Add watermark to exports for Starter — `src/components/project/design-studio/ExportPanel.tsx`**

- Import `useSubscriptionFeatures`
- Pass `plan` to the edge function body: `{ designId, plan: currentPlan }`
- For HTML fallback exports, inject a watermark footer div: "Generated by OptiRFP Free — Upgrade to remove" when plan is `starter`
- The server-side PDF function should also check plan (but that's an edge function change, noted below)

**6. Add watermark support to export edge function — `supabase/functions/export-proposal-pdf/index.ts`**

- Read `plan` from request body
- If plan is `starter`, inject watermark text into the HTML/PDF output

**7. Opportunity Search usage tracking for Growth tier**

Create `src/hooks/use-search-usage.ts`:
- Query `organization_usage_metrics` for `metric_type = 'opportunity_search'` and current month
- Return `{ searchesUsed, searchesRemaining, limit, isAtLimit }`
- Growth limit = 10, Business/Enterprise = unlimited (-1)

Update `src/pages/Opportunities.tsx`:
- Show counter badge: "3 of 10 searches remaining this month" for Growth users
- When `isAtLimit`, block search with upgrade nudge to Business
- After each search, increment the usage metric

**8. Enterprise CSM badge — `src/components/navigation/Navbar.tsx` or Dashboard**

- When plan is `enterprise`, show a small "Dedicated CSM" badge/indicator on the dashboard or account section

**9. Create `useFeatureGate` convenience hook — `src/hooks/use-feature-gate.ts`**

Thin wrapper around `useSubscriptionFeatures` that returns:
```ts
{ 
  isLocked: boolean,
  requiredPlan: string,
  showGate: () => void, // opens PlanComparisonModal
  plan: string
}
```
Used for imperative checks (e.g., button click handlers) vs the declarative `<FeatureGate>` component.

### Files touched
- **Modified**: `src/components/subscription/FeatureGate.tsx`, `src/pages/Opportunities.tsx`, `src/pages/ApiDocs.tsx`, `src/components/project/proposal-evaluation/ProposalEvaluation.tsx`, `src/components/project/design-studio/ExportPanel.tsx`
- **New**: `src/hooks/use-feature-gate.ts`, `src/hooks/use-search-usage.ts`
- **Modified (edge)**: `supabase/functions/export-proposal-pdf/index.ts` (watermark injection)

