

## Plan: Enhance Watermarking for Free Tier Exports

### Current State
- **ExportPanel.tsx** (Design Studio PDF): Already injects watermark for starter plan — both client-side HTML fallback and server-side edge function
- **AutoGeneratedProposal.tsx**: Exports raw markdown file and copies to clipboard — no watermark
- **CompiledView.tsx**: Copies compiled proposal to clipboard — no watermark
- **No Word/docx export exists** in the codebase — skip docx watermarking
- Watermark styling is currently `font-size:11px;color:#888` — needs update to match spec (10pt italic, 30% opacity)

### Changes

**1. Update watermark styling everywhere to match spec**
- Text: "Generated by OptiRFP Free — Upgrade to remove watermark"
- Style: `font-size:10pt; font-style:italic; color:rgba(0,0,0,0.3)` (30% opacity, light gray)
- Apply to: `ExportPanel.tsx` client-side fallback and `export-proposal-pdf/index.ts` server-side HTML

**2. Add watermark to AutoGeneratedProposal exports — `AutoGeneratedProposal.tsx`**
- Import `useSubscriptionFeatures` to get `plan`
- `handleExportProposal`: If starter, append watermark text line at bottom of markdown file: `\n\n---\n_Generated by OptiRFP Free — Upgrade to remove watermark_`
- `handleCopyToClipboard`: Same watermark appended to copied text for starter users
- Show upgrade toast after watermarked export/copy: "This export includes a watermark. Upgrade to remove it." with action button "Upgrade Now" linking to `/subscription`

**3. Add watermark to CompiledView copy — `CompiledView.tsx`**
- Import `useSubscriptionFeatures`
- In `handleCopy`, append watermark text for starter plan
- Show upgrade toast after copy

**4. Add in-app preview watermark overlay — `AutoGeneratedProposal.tsx`**
- For starter users, overlay a semi-transparent watermark bar at bottom of the proposal content `<div>` (the `max-h-[600px]` scrollable container)
- Style: fixed to bottom of container, 30% opacity, italic, with tooltip "Upgrade to remove watermark"

**5. Add in-app preview watermark to CompiledView — `CompiledView.tsx`**
- Same overlay approach on the rendered markdown container for starter users

**6. Trial period exemption**
- In both components, check `subscription?.status === 'trialing'` — if trialing, skip watermark (treat as paid)

### Files Touched
- **Modified**: `src/components/project/auto-proposal/AutoGeneratedProposal.tsx`
- **Modified**: `src/components/project/proposal-draft/components/CompiledView.tsx`
- **Modified**: `src/components/project/design-studio/ExportPanel.tsx`
- **Modified**: `supabase/functions/export-proposal-pdf/index.ts`

